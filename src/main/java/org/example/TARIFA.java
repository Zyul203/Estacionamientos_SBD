package org.example;

public interface TARIFA
{
   double calcular(int horas);
}
//En las interfaces por default todos los metodos son publicos, ya que una interfaz esta realizando contratos
//asi que es obvio que el contrato debe ser visible